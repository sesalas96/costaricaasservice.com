[build]
  base    = "frontends/cri-landing"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION       = "20"
  NPM_FLAGS          = "--no-audit --no-fund"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
